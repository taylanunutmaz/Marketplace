# Marketplace

## How to run

```bash
git clone https://github.com/taylanunutmaz/Marketplace.git
cd Marketplace
# do not forget to edit spring.datasource.url, spring.datasource.username, spring.datasource.password in application.properties with valid MySQL DB
./mvnw spring-boot:run
```
